# Micro service api architectur experiment

The main idea is to separate user management (user CRUD, reset password,
permissions) from content management. The plan is to use JWT token as glue between services. 

